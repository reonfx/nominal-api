export default function Home() {
  return (
    <div style={{ fontFamily: "system-ui, Arial", padding: 20 }}>
      <h1>Nominal API Working</h1>
      <p>API is deployed — use the /api endpoints.</p>
    </div>
  );
}
